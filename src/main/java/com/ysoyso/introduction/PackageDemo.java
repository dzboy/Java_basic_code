/*
    注意：
    - package 语句应该放在源文件的第一行
    - 在每个源文件中只能有一个包定义语句

    Java 包的命名规则：
    - 包名全部由小写字母（多个单词也全部小写）。
    - 如果包名包含多个层次，每个层次用“.”分割，比如com.ysoyso。
    - 包名一般由倒置的域名开头，比如域名ysoyso.com，则包名为com.ysoyso，不要有 www。
    - 自定义包不能以 java 开头。
 */
package com.ysoyso.introduction;

/**
 * package和import
 */
public class PackageDemo {
    public static void main(String[] args) {

    }
}
